# Stack app properties

app.prettyPrintJson=true
app.autoreloadVelocityTemplates=true
app.queryLoggingEnabled=true
app.exceptionDebuggingEnabled=true
app.pageCompressionEnabled=false

# Stack DB properties

jdbc.username=dispatch
jdbc.password=dispatch
jdbc.url=jdbc:postgresql://localhost:5432/dispatch
jdbc.driverClassName=org.postgresql.Driver
jdbc.schema=dispatch

# The maximum number of active connections that can be allocated from 
# this pool at the same time, or negative for no limit.
jdbc.maxActive=10

# The maximum number of connections that can remain idle in the pool,
# without extra ones being released, or negative for no limit.
jdbc.maxIdle=5

# Custom properties

example.property=This is an example property