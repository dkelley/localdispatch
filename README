To run the app in Eclipse, run com.xmog.web.ServerLauncher and specify JVM arguments "-Denv=dev -Duser.timezone=UTC -DstopOnEnterKeypress=true". 

To run the app from the command line, use "ant run".

To create a distributable zip file for QA/production deployment, use "ant dist".

Check out the nginx directory to see how to set it up for development and production deployment.